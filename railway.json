{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php -r \"echo 'PHP OK';\" && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/customer",
    "healthcheckTimeout": 300
  }
}
